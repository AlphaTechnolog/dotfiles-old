require'nvim-tree'.setup {
  disable_netrw = true,
  auto_close = false,
  open_on_tab = false,
  update_cwd = true,
  update_focused_file = {
    enable = true
  },
  view = {
    width = 28,
    height = 30,
    side = 'right',
    auto_resize = true
  }
}
