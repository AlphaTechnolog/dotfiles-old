#!/bin/bash

# This script requires playerctl, make sure you have installed it
# in your system

playerctl play-pause

# Trying with cmus-remote, some times I use cmus
if [[ $? != 0 ]]; then
  cmus-remote --pause
fi
